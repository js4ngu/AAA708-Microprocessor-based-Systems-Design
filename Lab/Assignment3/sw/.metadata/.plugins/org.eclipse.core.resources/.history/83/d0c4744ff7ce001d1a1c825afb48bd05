#include "sleep.h"
#include "lab0.h"


int aes_main() {
    uint32_t r3_value;
    asm volatile ("mov %[r3_value], r3" : [r3_value] "=r" (r3_value));

	if(r3_value == 0x00020000) usleep(100000);
	else usleep(500000);
	return 0;
}
